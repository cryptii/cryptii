
import Encoder from '../Encoder'

/**
 * Abstract Encoder foundation for homophonic substitution ciphers.
 * In these ciphers, plaintext letters map to more than one ciphertext symbol.
 * @abstract
 */
export default class HomophonicSubstitutionEncoder extends Encoder {
  // TODO needs implementation
}
