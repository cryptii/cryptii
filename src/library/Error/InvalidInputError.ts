
import GenericError from '../Error/GenericError'

/**
 * Errors occurring due to malformed, invalid or unexpected user input.
 * These are expected runtime errors which messages are displayed to the user.
 */
export default class InvalidInputError extends GenericError {

}
